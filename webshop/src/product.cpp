#include "product.hpp"

Product::Product(const std::string& name, double unitprice, double weight)
: name(name), unitprice(unitprice), weight(weight)
{
    //this->name = name;
    //this->unitprice = unitprice;
    //this->weight = weight;
}