#hier werden global für alle ordner alles eingestellt

cmake_minimum_required(VERSION 3.15)

project(RSE_SWE_SS26)

set(MODULE cpp_list)        # Set the name of the module to be built. Bei anderen Projekten muss hier der Name des Moduls angepasst werden.

add_subdirectory(${MODULE})